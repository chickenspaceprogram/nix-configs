# my nix config


mostly keeping this here as a backup in case i bork something. it should have most of the software i use.

also note that you need to run `mkdir -p /etc/nixos/firmware && cp /boot/asahi/{all_firmware.tar.gz,kernelcache*} /etc/nixos/firmware`  or something to that effect before running this! also the hardware config is pretty asahi specific ofc


THIS REPO HAS BEEN MIGRATED TO <https://codeberg.org/chickenspaceprogram/nix-configs>.
